<h1>Portfolio Items</h1>
<%= link_to 'Create new Item', new_portfolio_path %>

<% @portfolio_items.each do |portfolio_item| %>
	<p><%= portfolio_item.title %></p>
	<p><%= portfolio_item.subtitle %></p>
	<p><%= image_tag portfolio_item.thumb_image if !portfolio_item.thumb_image.nil? %></p>
	<%= link_to "Edit ", edit_portfolio_path(portfolio_item.id) %>  |
	<%= link_to "Show", portfolio_show_path(portfolio_item) %> |
	<%= link_to "Delete", portfolio_path(portfolio_item), method: :delete, data: {confirm: "Are you sure you want to delete this?"} %>
<% end %>